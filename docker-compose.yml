services:
  shopverse-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: shopverse-web:local
    container_name: shopverse-web
    ports:
      - "5173:5173"
    environment:
      NODE_ENV: production
      PORT: 5173
    restart: unless-stopped
